Usage is as follows

```
pacman -Syu --noconfirm
pacman --noconfirm -S git ostree python3 python-pip python-yaml sudo systemd
git clone https://github.com/ONL/arch-ostree.git
cd arch-ostree
./arch-ostree.py sample-config.yml master 1
```
